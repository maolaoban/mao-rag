{
  "name": "mao-rag",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "dev": "concurrently \"pnpm --filter mao-rag-server dev\" \"pnpm --filter mao-rag-client dev\"",
    "dev:server": "pnpm --filter mao-rag-server dev",
    "dev:frontend": "pnpm --filter mao-rag-client dev",
    "build": "pnpm --filter mao-rag-client build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "packageManager": "pnpm@10.5.0",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "zod": "^4.3.6"
  }
}
